% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coexpr_eval.R
\name{eval_null_gap}
\alias{eval_null_gap}
\title{Real vs. permuted-null edge-weight distribution}
\usage{
eval_null_gap(obs, cor_type = "spearman", n_perm = 20L, threshold = 0.3)
}
\arguments{
\item{obs}{ObsPointSet (see package description).}

\item{cor_type}{Correlation type: \code{"spearman"} (default) or \code{"pearson"}.}

\item{n_perm}{Number of permutations for the null distribution. Default \code{20L}.}

\item{threshold}{Absolute-correlation threshold for edge counting.
Default \code{0.3}.}
}
\value{
A one-row \code{data.frame} with columns: \code{null_gap_ratio}
(real_frac / perm_frac_mean; \code{Inf} if perm_frac_mean = 0),
\code{real_frac}, \code{perm_frac_mean}.
}
\description{
Compare the fraction of gene-pair edges with \verb{|cor| >= threshold} in real
data vs. an independently-shuffled null. For each of \code{n_perm} permutations,
the rows of \code{obs$matrix} are independently permuted (destroying covariation
structure), correlations are recomputed, and the suprathreshold fraction is
recorded. The null-gap ratio (\code{real_frac / perm_frac_mean}) is the primary
output: values well above 1 indicate genuine coexpression signal.
}
