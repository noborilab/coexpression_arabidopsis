% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coexpr_eval.R
\name{evaluate_obs_design}
\alias{evaluate_obs_design}
\title{Full prior-free evaluation harness for one observation-point design}
\usage{
evaluate_obs_design(
  bundle,
  design_fn,
  design_args = list(),
  cor_type = "spearman",
  norm_method = "cp10k_log",
  n_splithalf = 3L,
  run_downsample_depth = FALSE,
  run_downsample_cells = FALSE,
  splithalf_reps = 3L,
  heldout_folds = 5L,
  null_perm = 10L
)
}
\arguments{
\item{bundle}{InputBundle (see package description).}

\item{design_fn}{Function that maps a bundle to an ObsPointSet.}

\item{design_args}{Named list of extra arguments forwarded to \code{design_fn}.
Default \code{list()}.}

\item{cor_type}{Correlation type: \code{"spearman"} (default) or \code{"pearson"}.}

\item{norm_method}{Normalization method passed to \code{normalize_obs()}.
Default \code{"cp10k_log"}.}

\item{n_splithalf}{Number of split-half replicates. Pass \code{0L} to skip
split-half evaluation. Default \code{3L}.}

\item{run_downsample_depth}{Logical; if \code{TRUE}, run \code{eval_downsample_depth}
and append \code{downsample_depth_mat_cor_at_0.5}. Default \code{FALSE}.}

\item{run_downsample_cells}{Logical; if \code{TRUE}, run \code{eval_downsample_cells}
and append \code{downsample_cells_mat_cor_at_0.5}. Default \code{FALSE}.}

\item{splithalf_reps}{Alias for \code{n_splithalf} (kept for API compatibility).
If both are provided, \code{n_splithalf} takes precedence. Default \code{3L}.}

\item{heldout_folds}{Number of cross-validation folds for
\code{eval_heldout_predictivity}. Default \code{5L}.}

\item{null_perm}{Number of permutations for \code{eval_null_gap}. Default \code{10L}.}
}
\value{
A one-row \code{data.frame} with columns: \code{design_name}, \code{n_points},
\code{n_genes}, \code{eff_rank}, \code{n_visible}, \code{frac_visible},
\code{predictivity_mean_r2}, \code{predictivity_median_r2}, \code{null_gap_ratio},
\code{real_frac}, \code{perm_frac_mean}, \code{depth_leakage_rho},
\code{splithalf_mat_cor_mean}, \code{splithalf_mat_cor_sd},
\code{splithalf_jaccard_mean}, \code{splithalf_jaccard_sd}.
Plus \code{downsample_depth_mat_cor_at_0.5} if \code{run_downsample_depth = TRUE}.
Plus \code{downsample_cells_mat_cor_at_0.5} if \code{run_downsample_cells = TRUE}.
}
\description{
\strong{IMPORTANT CAVEAT:} Stability metrics (split-half, downsampling) alone
favour trivial designs (one huge observation point, or designs that only
resolve the dominant axis). They \strong{MUST} be read jointly with richness
metrics (effective rank, visible genes, held-out predictivity). Selection
should be made on the stability–richness Pareto front, not any single
metric.

Runs the full suite of prior-free evaluation metrics for a given
observation-point design:
\itemize{
\item Richness: \code{eval_effective_rank}, \code{eval_visible_genes},
\code{eval_heldout_predictivity}
\item Signal: \code{eval_null_gap}, \code{eval_depth_leakage}
\item Stability: \code{eval_splithalf} (controlled by \code{n_splithalf} / \code{splithalf_reps})
\item Optional: \code{eval_downsample_depth}, \code{eval_downsample_cells}
}

Each metric is wrapped in \code{tryCatch}; failures record \code{NA} with a warning
so that one failing metric does not abort the entire evaluation.
}
