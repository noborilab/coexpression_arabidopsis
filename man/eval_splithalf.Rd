% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coexpr_eval.R
\name{eval_splithalf}
\alias{eval_splithalf}
\title{Split-half reproducibility of a co-expression design}
\usage{
eval_splithalf(
  bundle,
  design_fn,
  design_args = list(),
  cor_type = "spearman",
  norm_method = "cp10k_log",
  n_reps = 5L,
  top_k_frac = 0.01
)
}
\arguments{
\item{bundle}{InputBundle (see package description).}

\item{design_fn}{Function that maps a bundle to an ObsPointSet.}

\item{design_args}{Named list of extra arguments forwarded to \code{design_fn}
after the bundle. Default \code{list()}.}

\item{cor_type}{Correlation type: \code{"spearman"} (default) or \code{"pearson"}.}

\item{norm_method}{Normalization method passed to \code{normalize_obs()}.
Default \code{"cp10k_log"}.}

\item{n_reps}{Number of random split-half replicates. Default \code{5L}.}

\item{top_k_frac}{Fraction of edges used for Jaccard comparison. Default \code{0.01}.}
}
\value{
A one-row \code{data.frame} with columns: \code{mat_cor_mean}, \code{mat_cor_sd},
\code{jaccard_mean}, \code{jaccard_sd}, \code{n_reps} (effective number of successful reps).
}
\description{
Randomly split cells in \code{bundle} into two equal halves \code{n_reps} times. For
each split, independently build an ObsPointSet and correlation matrix in each
half (using \code{.build_obs_from_bundle}). Agreement is measured by Pearson
correlation of the upper-triangle elements of the two correlation matrices
(\code{mat_cor}) and Jaccard of the top-\code{top_k_frac} edges (\code{jaccard}). Only
genes common to both halves are compared.

If a split fails (e.g. too few cells for the design), that rep is skipped
with a warning and the effective \code{n_reps} is reduced. If no rep succeeds,
all metric columns are \code{NA}.
}
