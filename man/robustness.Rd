% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robustness.R
\name{robustness}
\alias{robustness}
\title{Cross-Context Robustness Layer (optional)}
\description{
Computes cross-stratum and cross-dataset reproducibility scores for
co-expression edges. Enabled or disabled via \code{robustness.enabled} in config.

\strong{R_score method} (per-stratum fixed-evidence indicator aggregation):
\itemize{
\item Per-stratum Fisher z-transform: \code{z_s = atanh(rho_s)}, \code{SE_s = 1/sqrt(n_s - 3)}
\item Fixed-evidence indicator: \code{I_s = 1[z_s >= k * SE_s]} where \code{k ~ 1.64}
(calibrated on positive controls; small strata require larger rho to qualify)
\item Weighted aggregate: \code{R_score = sum(w_s * I_s) / sum(w_s)},
\code{w_s = sqrt(min(n_s, 30) - 3)}
\item Null: analytic normal approximation to weighted Poisson-binomial
(pi_s = P(Z >= k) under H0, same for all strata) → BH-FDR.
}

\strong{Benchmark note:} R_score did not outperform naive Spearman on GO co-functional
pair recovery (AUPRC ~0.20 for all methods). Value is interpretability and
context annotation, not global network improvement.
}
