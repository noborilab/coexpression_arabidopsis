% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpret.R
\name{build_wgcna_modules}
\alias{build_wgcna_modules}
\title{Build WGCNA co-expression modules from a robustness-filtered edge set}
\usage{
build_wgcna_modules(
  rob,
  network_list,
  r_score_min = 0,
  soft_power = NULL,
  merge_cut = 0.25,
  min_module_size = 30,
  sub_merge_cut = 0.1
)
}
\arguments{
\item{rob}{RobustnessResult from \code{\link[=compute_robustness]{compute_robustness()}}.}

\item{network_list}{Named list of NetworkResult (same strata as \code{rob}).
Used by downstream annotation functions (context, preservation).}

\item{r_score_min}{Minimum R_score to include an edge. Default \code{0} (all
edges). Caller decides the threshold; this function does not filter
further.}

\item{soft_power}{WGCNA soft-thresholding power. \code{NULL} (default) =
auto-pick by scale-free fit with \code{pickSoftThreshold.fromSimilarity}.}

\item{merge_cut}{Module merge threshold (height). Default \code{0.25}. Lower
value = fewer, coarser modules.}

\item{min_module_size}{Minimum number of genes per module. Default \code{30}.}

\item{sub_merge_cut}{Merge threshold for sub-modules (finer granularity).
Default \code{0.10}. \code{NULL} = skip sub-module construction.}
}
\value{
ModuleInput (named list; see \code{docs/OUTPUT_SCHEMA.md}).
}
\description{
Constructs an adjacency matrix from the robustness-weighted mean partial
correlations, applies WGCNA soft-thresholding, TOM, and hierarchical
clustering to produce coarse top-level modules and optional finer
sub-modules.
}
