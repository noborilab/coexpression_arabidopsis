% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coexpr_eval.R
\name{eval_heldout_predictivity}
\alias{eval_heldout_predictivity}
\title{Cross-validated guilt-by-association predictivity}
\usage{
eval_heldout_predictivity(
  obs,
  k_partners = 10L,
  n_folds = 5L,
  cor_type = "spearman"
)
}
\arguments{
\item{obs}{ObsPointSet (see package description).}

\item{k_partners}{Number of top-correlated partner genes to use for
prediction. Default \code{10L}.}

\item{n_folds}{Number of cross-validation folds. Default \code{5L}.}

\item{cor_type}{Correlation type for computing partner correlations:
\code{"spearman"} (default) or \code{"pearson"}.}
}
\value{
A one-row \code{data.frame} with columns: \code{predictivity_mean_r2},
\code{predictivity_median_r2}.
}
\description{
Partition observation points into \code{n_folds} folds. For each fold, compute
gene-gene correlations on training points, identify the top-\code{k_partners}
correlated partners for each gene (excluding self), predict held-out
expression as the absolute-value-weighted mean of partner expression, and
compute per-gene R². Returns mean and median R² across genes.

This is the single most informative prior-free metric: it rewards both real
AND broad covariation structure simultaneously.

\strong{Implementation notes:}
\itemize{
\item \code{set.seed} is not called internally; callers may set the seed before
calling.
\item If \code{n_points < 2 * n_folds}, \code{n_folds} is reduced to
\code{max(2L, floor(n_points / 3L))}.
\item Only genes with \code{ss_tot > 0} in the held-out fold contribute to R².
\item Prediction: \verb{pred_g = sum(|w_gj| * expr_j) / sum(|w_gj|)} for the top-k
partners j.
}
}
