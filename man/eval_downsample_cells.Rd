% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coexpr_eval.R
\name{eval_downsample_cells}
\alias{eval_downsample_cells}
\title{Cell-downsampling robustness}
\usage{
eval_downsample_cells(
  bundle,
  design_fn,
  design_args = list(),
  fractions = c(1, 0.75, 0.5, 0.25),
  cor_type = "spearman",
  norm_method = "cp10k_log"
)
}
\arguments{
\item{bundle}{InputBundle (see package description).}

\item{design_fn}{Function that maps a bundle to an ObsPointSet.}

\item{design_args}{Named list of extra arguments forwarded to \code{design_fn}.
Default \code{list()}.}

\item{fractions}{Numeric vector of cell-fraction levels in (0, 1].
Default \code{c(1.0, 0.75, 0.5, 0.25)}.}

\item{cor_type}{Correlation type: \code{"spearman"} (default) or \code{"pearson"}.}

\item{norm_method}{Normalization method passed to \code{normalize_obs()}.
Default \code{"cp10k_log"}.}
}
\value{
A \code{data.frame} with columns \code{fraction}, \code{mat_cor}, \code{jaccard}.
}
\description{
Sample cells to each fraction in \code{fractions}, rebuild the ObsPointSet and
correlation matrix, and compare each result to the full-cell result
(fraction = 1.0 if included in \code{fractions}, otherwise the result from the
full bundle). Agreement is measured by Pearson correlation of upper-triangle
elements and Jaccard of the top-1\% edges.
}
