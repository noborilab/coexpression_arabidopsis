% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coexpr_eval.R
\name{eval_depth_leakage}
\alias{eval_depth_leakage}
\title{Depth / abundance confounding diagnostic}
\usage{
eval_depth_leakage(obs, threshold = 0.3)
}
\arguments{
\item{obs}{ObsPointSet (see package description).}

\item{threshold}{Absolute-correlation threshold used to define network edges.
Default \code{0.3}.}
}
\value{
A one-row \code{data.frame} with columns: \code{depth_leakage_rho} (Spearman
rho between degree and mean expression), \code{n_genes} (integer).
}
\description{
Computes the Spearman correlation between each gene's network degree (number
of edges with \verb{|cor| >= threshold}) and its mean expression across
observation points. A high positive correlation indicates that the design or
normalization is leaking a sequencing-depth / abundance axis: hubs are
simply highly expressed genes rather than genuinely co-regulated ones.
}
