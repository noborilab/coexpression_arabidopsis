% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robustness.R
\name{characterize_condition_pattern}
\alias{characterize_condition_pattern}
\title{Characterize each gene pair by its cross-condition activity pattern}
\usage{
characterize_condition_pattern(
  rob,
  network_list,
  condition_order = c("Mock", "DC3000", "AvrRpt2", "AvrRpm1")
)
}
\arguments{
\item{rob}{RobustnessResult from \code{\link[=compute_robustness]{compute_robustness()}}. \verb{$pair_scores} must
contain \verb{I_<condition>} columns for each element of \code{condition_order}.}

\item{network_list}{Named list of NetworkResult (one per condition). Names
must include each element of \code{condition_order}. Conditions absent from the
list are treated as weight = 0 for all pairs.}

\item{condition_order}{Character vector fixing the bit order for the pattern
string. Default \code{c("Mock","DC3000","AvrRpt2","AvrRpm1")}.}
}
\value{
\code{data.frame}, one row per pair in \code{rob$pair_scores}, with columns:
\code{gene_id_A}, \code{gene_id_B};
\verb{I_<condition>} (integer 0/1, from rob);
\code{pattern} (S-character bit string in condition_order);
\code{pattern_label} (category string);
\code{n_conditions_active} (integer 0–S);
\verb{w_<condition>} (pcor weight from per-condition network, or 0 if absent);
\code{w_max}, \code{w_min}, \code{w_range}, \code{w_mean}, \code{specificity_index}.
}
\description{
Produces BOTH a discrete binary pattern label and the continuous
per-condition effect sizes, so pairs can be grouped by pattern OR clustered
on the continuous matrix. The discrete pattern is derived mechanically from
the I_s indicators already present in \code{rob$pair_scores}. Named labels
("pan_pathogen", "ETI_shared", etc.) are convenient handles for bit patterns;
they do not imply any biological interpretation.
}
\details{
\strong{Pattern label lookup (default 4-condition order
Mock / DC3000 / AvrRpt2 / AvrRpm1):}
\itemize{
\item \code{"1111"} → \code{"constitutive_all"}
\item \code{"0111"} → \code{"pan_pathogen"} (DC3000 + AvrRpt2 + AvrRpm1, not Mock)
\item \code{"0011"} → \code{"ETI_shared"} (AvrRpt2 + AvrRpm1)
\item \code{"0000"} → \code{"none"}
\item Single-bit patterns → \code{"single_<condition>"}
\item All other patterns → \code{"mixed_<pattern>"}
}

\strong{Specificity index:} \code{(w_max - w_mean_of_others) / (w_max + epsilon)},
where \code{w_mean_of_others = (sum(w) - w_max) / (S - 1)} and \code{epsilon = 1e-6}.
Approaches 1 when signal is concentrated in one condition; approaches 0
when uniform across all conditions.
}
