% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coexpr_eval.R
\name{eval_downsample_depth}
\alias{eval_downsample_depth}
\title{Depth-downsampling robustness}
\usage{
eval_downsample_depth(
  bundle,
  design_fn,
  design_args = list(),
  fractions = c(1, 0.5, 0.25, 0.1),
  cor_type = "spearman",
  norm_method = "cp10k_log"
)
}
\arguments{
\item{bundle}{InputBundle. Must have non-NULL \code{counts_raw}; if \code{NULL},
the function warns and returns \code{NULL}.}

\item{design_fn}{Function that maps a bundle to an ObsPointSet.}

\item{design_args}{Named list of extra arguments forwarded to \code{design_fn}.
Default \code{list()}.}

\item{fractions}{Numeric vector of thinning fractions in (0, 1].
Default \code{c(1.0, 0.5, 0.25, 0.1)}.}

\item{cor_type}{Correlation type: \code{"spearman"} (default) or \code{"pearson"}.}

\item{norm_method}{Normalization method passed to \code{normalize_obs()}.
Default \code{"cp10k_log"}.}
}
\value{
A \code{data.frame} with columns \code{fraction}, \code{mat_cor}, \code{jaccard},
or \code{NULL} if \code{bundle$counts_raw} is \code{NULL}.
}
\description{
Binomially thin \code{bundle$counts_raw} to each fraction in \code{fractions},
rebuild the ObsPointSet and correlation matrix, and compare each result to
the full-depth result (fraction = 1.0 if included in \code{fractions}, otherwise
the result from the unthinned bundle).

\strong{Note on thinning and normalization:} This function thins \code{counts_raw}
only. When the design uses \code{aggregation = "sum"}, the thinned \code{counts_raw}
propagates correctly through the sum-aggregation path. When the design uses
\code{aggregation = "mean"}, the log-normalized \code{counts} matrix is unchanged, so
thinning has no effect; a warning is emitted in this case if detected via
\code{design_args$aggregation}.
}
